import { Router } from "express";

/**
 * =====================================
 * ROUTES RELANCE — PHASE 1 (STUB)
 * =====================================
 * Relances automatiques IA prévues
 * en PHASE 2.
 */

const router = Router();

export default router;
